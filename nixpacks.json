{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs-18_x"]
    },
    "install": {
      "cmds": [
        "cd backend && npm install --production",
        "cd backend && npx playwright install chromium --with-deps",
        "cd backend && npx knex migrate:latest --knexfile knexfile.js"
      ]
    }
  },
  "start": {
    "cmd": "cd backend && node server.js"
  }
}
