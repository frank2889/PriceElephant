{% assign primary_color = settings.primary_color | default: '#000000' %}
<section class="business-product-analysis" style="--primary-color: {{ primary_color }};">
  <div class="container">
    <div class="business-header">
      <h1 class="business-title">PRICING INTELLIGENCE</h1>
      <p class="business-subtitle">Product: {{ product.title }}</p>
      <p class="business-sku">SKU: {{ product.selected_or_first_available_variant.sku | default: 'N/A' }}</p>
    </div>

    <div class="business-analytics-grid">
      <!-- Current Pricing Strategy -->
      <div class="analytics-card primary-card">
        <h3 class="card-title">JOUW HUIDIGE STRATEGIE</h3>
        <div class="price-display">
          <span class="current-price">{{ product.price | money }}</span>
          <span class="price-label">Huidige verkoopprijs</span>
        </div>
        <div class="margin-info">
          <div class="margin-row">
            <span>Inkoopprijs:</span>
            <span>{{ product.compare_at_price | money | default: 'Niet ingesteld' }}</span>
          </div>
          <div class="margin-row">
            <span>Marge:</span>
            <span class="margin-percentage">
              {% if product.compare_at_price and product.price %}
                {{ product.price | minus: product.compare_at_price | divided_by: product.price | times: 100 | round: 1 }}%
              {% else %}
                N/A
              {% endif %}
            </span>
          </div>
        </div>
      </div>

      <!-- Competitor Analysis -->
      <div class="analytics-card">
        <h3 class="card-title">CONCURRENTIE ANALYSE</h3>
        <div id="competitor-data-{{ product.id }}" class="competitor-list">
          <div class="competitor-item">
            <span class="competitor-name">Amazon</span>
            <span class="competitor-price loading">Loading...</span>
            <span class="price-diff">--</span>
          </div>
          <div class="competitor-item">
            <span class="competitor-name">Bol.com</span>
            <span class="competitor-price loading">Loading...</span>
            <span class="price-diff">--</span>
          </div>
          <div class="competitor-item">
            <span class="competitor-name">Coolblue</span>
            <span class="competitor-price loading">Loading...</span>
            <span class="price-diff">--</span>
          </div>
        </div>
        <div class="market-position">
          <strong>Marktpositie: <span id="market-rank">Berekenen...</span></strong>
        </div>
      </div>

      <!-- AI Recommendations -->
      <div class="analytics-card recommendation-card">
        <h3 class="card-title">AI AANBEVELINGEN</h3>
        <div class="recommendation-content">
          <div class="recommendation-item">
            <span class="rec-icon">💡</span>
            <div class="rec-details">
              <strong>Optimale prijs: €{{ product.price | plus: 25 | money_without_currency }}</strong>
              <p>+5% marge mogelijk zonder marktaandeel te verliezen</p>
            </div>
          </div>
          <div class="recommendation-item">
            <span class="rec-icon">📊</span>
            <div class="rec-details">
              <strong>Trend: Stabiel</strong>
              <p>Concurrentenprijzen zijn afgelopen 7 dagen stabiel</p>
            </div>
          </div>
        </div>
        <button type="button" class="btn-business btn-primary" onclick="applyRecommendation()">
          PRIJS BIJWERKEN
        </button>
      </div>

      <!-- Quick Actions -->
      <div class="analytics-card actions-card">
        <h3 class="card-title">ACTIES</h3>
        <div class="action-buttons">
          <button type="button" class="btn-business btn-secondary" onclick="setAlert()">
            🔔 ALERT INSTELLEN
          </button>
          <button type="button" class="btn-business btn-secondary" onclick="viewHistory()">
            📈 PRIJSHISTORIE
          </button>
          <button type="button" class="btn-business btn-secondary" onclick="exportData()">
            📊 EXPORT DATA
          </button>
        </div>
      </div>
    </div>

    <!-- Product Details for Business Context -->
    {% if section.settings.show_business_details %}
    <div class="business-product-details">
      <h3>PRODUCT INFORMATIE</h3>
      <div class="product-meta">
        <div class="meta-item">
          <strong>Voorraad:</strong> 
          <span class="stock-level">{{ product.selected_or_first_available_variant.inventory_quantity | default: 'Onbekend' }}</span>
        </div>
        <div class="meta-item">
          <strong>Verkocht (30d):</strong>
          <span class="sales-data">Verbind analytics voor data</span>
        </div>
        <div class="meta-item">
          <strong>Conversie:</strong>
          <span class="conversion-rate">Verbind analytics voor data</span>
        </div>
      </div>
      {% if product.description != blank %}
      <div class="product-description">
        <strong>Beschrijving:</strong>
        <p>{{ product.description | truncate: 200 }}</p>
      </div>
      {% endif %}
    </div>
    {% endif %}
  </div>
</section>

<script>
// B2B Product Intelligence Functions
function applyRecommendation() {
  // Integrate with Shopify Admin API to update pricing
  alert('Prijs bijwerken functionaliteit - integratie met Shopify Admin API');
}

function setAlert() {
  alert('Alert instellen voor prijswijzigingen concurrenten');
}

function viewHistory() {
  alert('Prijshistorie dashboard openen');
}

function exportData() {
  alert('Data export naar Excel/CSV');
}

// Load competitor data (mock for demo)
document.addEventListener('DOMContentLoaded', function() {
  const productId = {{ product.id }};
  
  // Simulate API call to load competitor prices
  setTimeout(() => {
    const competitorData = [
      { name: 'Amazon', price: {{ product.price | plus: 20 }}, element: 0 },
      { name: 'Bol.com', price: {{ product.price | minus: 15 }}, element: 1 },
      { name: 'Coolblue', price: {{ product.price | plus: 10 }}, element: 2 }
    ];
    
    const competitors = document.querySelectorAll('#competitor-data-{{ product.id }} .competitor-item');
    const currentPrice = {{ product.price | divided_by: 100.0 }};
    
    competitorData.forEach((comp, index) => {
      if (competitors[index]) {
        const priceElement = competitors[index].querySelector('.competitor-price');
        const diffElement = competitors[index].querySelector('.price-diff');
        
        priceElement.textContent = '€' + (comp.price / 100).toFixed(2);
        priceElement.classList.remove('loading');
        
        const diff = comp.price / 100 - currentPrice;
        diffElement.textContent = (diff > 0 ? '+' : '') + diff.toFixed(2);
        diffElement.className = 'price-diff ' + (diff > 0 ? 'higher' : diff < 0 ? 'lower' : 'same');
      }
    });
    
    // Update market position
    const marketRank = document.getElementById('market-rank');
    if (marketRank) {
      marketRank.textContent = '#2 van 4 (Competitief gepositioneerd)';
    }
  }, 1500);
});
</script>

<style>
.business-product-analysis {
  padding: 2rem 0;
  background: #ffffff;
  color: #000000;
}

.business-header {
  text-align: center;
  margin-bottom: 3rem;
  padding-bottom: 2rem;
  border-bottom: 1px solid #333;
}

.business-title {
  font-size: 3rem;
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: -1px;
  margin-bottom: 1rem;
}

.business-subtitle {
  font-size: 1.2rem;
  font-weight: 600;
  margin-bottom: 0.5rem;
}

.business-sku {
  font-size: 0.9rem;
  color: #666;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.business-analytics-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
  margin-bottom: 3rem;
}

.analytics-card {
  background: #f8f8f8;
  border: 1px solid #333;
  padding: 2rem;
  transition: all 0.3s ease;
}

.analytics-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 20px rgba(0,0,0,0.1);
}

.primary-card {
  background: #000;
  color: #fff;
}

.recommendation-card {
  background: #f0f8ff;
  border-color: #000;
}

.card-title {
  font-size: 1.1rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 1.5rem;
  border-bottom: 2px solid currentColor;
  padding-bottom: 0.5rem;
}

.price-display {
  text-align: center;
  margin-bottom: 2rem;
}

.current-price {
  display: block;
  font-size: 2.5rem;
  font-weight: 900;
  margin-bottom: 0.5rem;
}

.price-label {
  display: block;
  font-size: 0.9rem;
  opacity: 0.8;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.margin-info {
  border-top: 1px solid rgba(255,255,255,0.3);
  padding-top: 1rem;
}

.margin-row {
  display: flex;
  justify-content: space-between;
  margin-bottom: 0.5rem;
  font-weight: 500;
}

.competitor-list {
  margin-bottom: 1.5rem;
}

.competitor-item {
  display: grid;
  grid-template-columns: 1fr auto auto;
  gap: 1rem;
  padding: 0.75rem 0;
  border-bottom: 1px solid #eee;
  align-items: center;
}

.competitor-name {
  font-weight: 600;
}

.competitor-price.loading {
  color: #999;
  font-style: italic;
}

.price-diff.higher {
  color: #d73527;
}

.price-diff.lower {
  color: #28a745;
}

.price-diff.same {
  color: #6c757d;
}

.market-position {
  background: #333;
  color: #fff;
  padding: 1rem;
  text-align: center;
  font-size: 0.9rem;
  text-transform: uppercase;
  letter-spacing: 1px;
}

.recommendation-content {
  margin-bottom: 2rem;
}

.recommendation-item {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  margin-bottom: 1.5rem;
}

.rec-icon {
  font-size: 1.5rem;
  flex-shrink: 0;
}

.rec-details strong {
  display: block;
  margin-bottom: 0.25rem;
  font-size: 1.1rem;
}

.action-buttons {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.btn-business {
  padding: 0.75rem 1.5rem;
  border: 2px solid #000;
  background: transparent;
  color: #000;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 1px;
  cursor: pointer;
  transition: all 0.2s ease;
  width: 100%;
}

.btn-business:hover {
  background: #000;
  color: #fff;
}

.btn-primary {
  background: #000;
  color: #fff;
}

.btn-primary:hover {
  background: #333;
}

.business-product-details {
  background: #f8f8f8;
  padding: 2rem;
  border: 1px solid #333;
}

.business-product-details h3 {
  font-size: 1.3rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 1.5rem;
  border-bottom: 2px solid #000;
  padding-bottom: 0.5rem;
}

.product-meta {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1rem;
  margin-bottom: 2rem;
}

.meta-item {
  padding: 1rem;
  background: #fff;
  border: 1px solid #ddd;
}

.stock-level,
.sales-data,
.conversion-rate {
  color: #666;
  font-weight: 500;
}

.product-description {
  background: #fff;
  padding: 1.5rem;
  border: 1px solid #ddd;
}

@media (max-width: 768px) {
  .business-analytics-grid {
    grid-template-columns: 1fr;
  }
  
  .business-title {
    font-size: 2rem;
  }
  
  .product-meta {
    grid-template-columns: 1fr;
  }
}
</style>

{% schema %}
{
  "name": "Business Product Intelligence",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_business_details",
      "label": "Show business product details",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_auto_pricing",
      "label": "Enable automatic pricing updates",
      "default": false
    },
    {
      "type": "select",
      "id": "competitor_sources",
      "label": "Competitor data sources",
      "options": [
        {
          "value": "all",
          "label": "All sources"
        },
        {
          "value": "major",
          "label": "Major retailers only"
        },
        {
          "value": "custom",
          "label": "Custom selection"
        }
      ],
      "default": "major"
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "Business Product Intelligence"
    }
  ]
}
{% endschema %}
